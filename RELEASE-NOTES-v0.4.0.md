# Lumi v0.4.0 发布说明

> 日期：2026-07-30
> 标签：`v0.4.0`
> 主题：竞品数据导入（获客钩子）

## 新增功能

### 数据导入（设置 → 数据 → 导入数据）
让用户从其他 App 迁移到 Lumi，直接对冲竞品「数据锁死」的信任坑。

- **Lumi 自有 JSON 回灌**：与「导出数据」对称，可把 Lumi 备份原样导入（含 profile 偏好）。
- **通用 CSV 导入**：靠列名同义词字典启发式识别，覆盖 Flo / Clue / 经期助手 等常见导出格式（英文 + 中文表头）。
  - 自动识别：日期、经期开始/结束、流量、心情、精力、睡眠、症状、备注。
  - 症状名 → Lumi symptom id 映射（双语同义词，如 `经痛/痛经/cramps → cramps`）。
  - 流量/心情/精力文本 → 枚举映射（含「轻/中/重」「开心/难过」等描述词）。
  - 多日期格式兼容：`YYYY-MM-DD` / `YYYY/MM/DD` / `MM/DD/YYYY` / `DD/MM/YYYY` / `2026年7月24日`。
- **预览确认**：导入前展示检测到的格式、将导入的条数、解析告警（未识别列 / 跳过的行），确认后才写入。
- **合并策略（非破坏）**：按日期 upsert，不覆盖已有记录；经期按开始日去重，日记按日 upsert。

## 修复 / 打磨
- 修正 CSV 解析中「普通日志行被误判为周期」的逻辑（仅当含开始日/结束日/流量时才建周期记录）。

## 测试
- 新增 `src/shared/lib/import.test.ts`（26 个用例）：日期/流量/症状映射、CSV 解析、JSON 回灌、格式探测、警告收集。
- 全量 **76 个测试通过**（原 50 + 新增 26）。

## 已知边界（待样本精修）
- Flo / Clue / 经期助手 的**精确列布局**未逐家验证（无真实样本）；当前为通用同义词匹配 + 手动预览核对。
- 提供任一家真实导出样本后，可补「精确预设」以提升一键命中率。

## 部署
- 推送到 `main` 触发 GitHub Actions → Cloudflare Pages（Direct Upload）自动部署。
- 包版本 `0.3.1 → 0.4.0`，`APP_LAST_UPDATED → 2026-07-30`。
