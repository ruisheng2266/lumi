# Lumi v0.5.0 发布说明

> 发布日期：2026-07-31
> 定位：纯本地功能（Free 全免费），延续"本地优先、数据归你、永远免费"原则。

## 本版本新增

### 1. 多年趋势 & 症状-阶段相关性（洞察深化）
- **周期长度长期趋势图**：每个周期间隔随时间变化，帮助发现长期规律（如围绝经期的间隔拉长）。
- **症状-阶段相关性图**：用堆叠柱状图展示各症状在经期 / 卵泡期 / 排卵期 / 黄体期的分布，直觉看出"某症状更常出现在哪个阶段"。
- 挂载于「洞察」页，与原有心情/精力/睡眠趋势、症状频率图并列。

### 2. 围绝经期专属工具
- 特殊生理场景新增 **「围绝经期」** 标记（在设置中添加，与怀孕/绝经等并列）。
- 围绝经期**不抑制经期预测**（过渡期仍有月经），仅在「今日」页给出温和提示：周期可能变得更不规律，预测仅供参考。
- 新增专属症状 **潮热 🔥 / 盗汗 🌙**，纳入症状频率与相关性统计。
- 新增「围绝经期模式」洞察：汇总潮热/盗汗记录，提示过渡进展与就医参考。

### 3. 备孕模式基础（BBT 基础体温）
- 日记新增 **基础体温（℃）** 录入字段（月经/精力/睡眠之后）。
- 「洞察」页新增 **BBT 曲线图**：绘制体温随时间变化，并基于"持续性升温"（前 6 天基线 + ≥0.2℃ 且连续 2 天维持）**推测排卵升温日**（非诊断）。
- 温度数据随导出 JSON 自然备份、随导入自然回灌。

### 4. 中立健康科普
- 新增 **「健康科普」** 页（底部导航第 5 项）：本地中立科普卡片——认识月经周期、围绝经期、备孕与基础体温、不规律周期。
- 全篇含 **非诊断 / 免责声明** 与参考来源（ACOG、妇产科学），强化"诚实有帮助"。

## 兼容性 / 数据模型
- `DailyLog` 新增可选字段 `bbt`（基础体温，℃）；`LifeEventType` 新增 `perimenopause`。均为**向后兼容**的可选字段，旧数据无需迁移。
- 竞品 CSV 导入新增 `bbt` 列识别与 `hotFlash`/`nightSweat` 症状同义词映射。

## 测试与质量
- 新增 `fertility.test.ts`（BBT 排卵检测）、`trends.test.ts`（周期趋势/相关性）单测；a11y 自动审计覆盖扩展至 7 个主页面（含 Education）。
- 全量 **107 测试通过**，`tsc` 类型检查通过，构建成功（LongTermCharts 已代码分割，不拖慢首屏）。

## 路线图调整
- 原计划 v0.5 的「i18n 扩展 ja/ko/zh-TW」经用户决定**移除**，不再纳入路线图。

## 升级方式
GitHub Actions 自动部署；刷新 lumi365.com 即生效。本地数据不受影响。
